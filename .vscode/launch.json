{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch WebExtension",
            "type": "firefox",
            "request": "launch",
            "reAttach": false,
            "addonPath": "${workspaceFolder}"
        }
    ]
}
